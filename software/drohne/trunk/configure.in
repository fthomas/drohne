AC_INIT(drohne, 0.2.0)
AC_CANONICAL_SYSTEM
AM_INIT_AUTOMAKE(drohne, 0.2.0)
AM_MAINTAINER_MODE

AC_PROG_INSTALL

dnl check for pkg-config
AC_PATH_PROG(PKG_CONFIG, pkg-config, no)
if test "x$PKG_CONFIG" = "xno"; then
    AC_MSG_ERROR([You need to install pkg-config])
fi
